# --- Web framework -------------------------------------------------------
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.20
pydantic>=2.9.0
pydantic-settings>=2.6.0

# --- Document parsing / rendering ---------------------------------------
PyMuPDF>=1.24.0          # primary renderer + text/table/image extraction (no external binaries)
pdf2image>=1.17.0        # optional alternative renderer (requires poppler)
pypdf>=5.1.0             # lightweight text-layer probing
python-docx>=1.1.2       # DOCX reference extraction
Pillow>=11.0.0

# --- GLM 4.6 client ------------------------------------------------------
openai>=1.55.0           # OpenAI-compatible async client used for Z.AI / BigModel
zhipuai>=2.1.5           # optional native SDK (not required at runtime)
httpx>=0.27.0

# --- Metrics -------------------------------------------------------------
rapidfuzz>=3.10.0        # fast Levenshtein; pure-python fallback included

# --- Tests ---------------------------------------------------------------
pytest>=8.3.0
pytest-asyncio>=0.24.0
